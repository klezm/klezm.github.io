# Satteri (pulled in by starlight-blog) loads its native binding by bare
# specifier from Astro's bundled prerender chunk, where pnpm's strict, symlinked
# node_modules layout cannot resolve it. Hoisting the platform packages to the
# root makes them visible from that path.
public-hoist-pattern[]=@bruits/*
